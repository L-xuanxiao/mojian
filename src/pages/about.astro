---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="关于" description="关于墨笺与写作者。">
  <section class="about page-shell">
    <aside aria-hidden="true">一方小笺<br />几笔闲墨</aside>
    <div class="about-content">
      <p class="lead">关于墨笺</p>
      <h1>在喧闹之外，留一页安静。</h1>
      <div class="copy">
        <p>
          墨笺是一处记录生活的小站。写走过的路、读过的书，也写窗前的一场雨和寻常的一顿饭。
        </p>
        <p>
          这里不追逐更新频率，只希望多年以后再翻开时，还能想起当时的光线、气味与心情。
        </p>
        <blockquote>愿每一次落笔，都能为流逝的日子留下微光。</blockquote>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .about {
    display: grid;
    min-height: 65vh;
    grid-template-columns: minmax(4rem, 10rem) minmax(0, 44rem);
    justify-content: center;
    gap: clamp(2rem, 8vw, 7rem);
    padding-top: clamp(5rem, 11vw, 9rem);
  }

  aside {
    justify-self: end;
    padding-top: 0.5rem;
    color: var(--color-pine);
    font-size: 1.05rem;
    letter-spacing: 0.28em;
    line-height: 2;
    writing-mode: vertical-rl;
  }

  .lead {
    margin: 0 0 1rem;
    color: var(--color-cinnabar);
    letter-spacing: 0.25em;
  }

  h1 {
    max-width: 10em;
    margin: 0;
    font-size: clamp(2.4rem, 6vw, 4.6rem);
    font-weight: 500;
    letter-spacing: 0.1em;
    line-height: 1.45;
  }

  .copy {
    margin-top: 3rem;
    color: var(--color-ink-soft);
    font-size: 1.05rem;
    line-height: 2;
    letter-spacing: 0.06em;
  }

  blockquote {
    margin: 2.5rem 0 0;
    padding: 1.5rem 0 1.5rem 1.5rem;
    border-left: 2px solid var(--color-cinnabar);
    color: var(--color-ink);
  }

  @media (max-width: 640px) {
    .about {
      grid-template-columns: 1fr;
    }

    aside {
      display: none;
    }
  }
</style>
