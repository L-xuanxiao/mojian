---
import { Image } from "astro:assets";
import inkMountains from "../assets/ink-mountains.png";
import BaseLayout from "../layouts/BaseLayout.astro";

const BASE = import.meta.env.BASE_URL;
---

<style>

    .section {
      position: relative;
      scroll-margin-top: 76px;
      padding: clamp(92px, 10vw, 150px) 0;
    }

    .section-heading {
      display: grid;
      grid-template-columns: minmax(0, 0.9fr) minmax(280px, 0.55fr);
      align-items: end;
      gap: 48px;
      margin-bottom: clamp(48px, 6vw, 78px);
    }

    .section-kicker {
      display: flex;
      align-items: center;
      gap: 12px;
      margin: 0 0 13px;
      color: var(--cinnabar);
      font-family: var(--serif);
      font-size: 0.76rem;
      letter-spacing: 0.22em;
    }

    .section-kicker::before {
      content: "";
      width: 34px;
      height: 1px;
      background: currentColor;
      opacity: 0.72;
    }

    .section-title {
      margin: 0;
      font-family: var(--serif);
      font-size: clamp(2.45rem, 5vw, 4.25rem);
      font-weight: 500;
      line-height: 1.12;
      letter-spacing: 0.1em;
    }

    .section-note {
      margin: 0;
      color: var(--ink-soft);
      font-family: var(--serif);
      font-size: clamp(1rem, 1.4vw, 1.13rem);
      line-height: 2;
    }

    .brush-divider {
      position: absolute;
      left: 50%;
      bottom: -14px;
      width: min(74vw, 960px);
      height: 42px;
      transform: translateX(-50%);
      opacity: 0.26;
      pointer-events: none;
    }

    /* =========================
       首页主视觉：水墨远山
    ========================== */
    .hero {
      position: relative;
      min-height: 600px;
      height: clamp(600px, 68svh, 720px);
      display: grid;
      align-items: center;
      padding: 118px 0 76px;
      isolation: isolate;
      overflow: hidden;
    }

    .hero::before {
      content: "";
      position: absolute;
      inset: 0;
      z-index: -3;
      background: linear-gradient(90deg, rgba(250, 248, 242, 0.88) 0%, rgba(250, 248, 242, 0.7) 43%, rgba(250, 248, 242, 0.3) 72%, rgba(250, 248, 242, 0.5) 100%);
    }

    .hero::after {
      content: "";
      position: absolute;
      inset: 0;
      z-index: -2;
      background: linear-gradient(180deg, rgba(247, 244, 235, 0.08), transparent 55%, rgba(242, 238, 227, 0.72));
      pointer-events: none;
    }

    .hero-grid {
      position: relative;
      z-index: 4;
      display: grid;
      grid-template-columns: minmax(0, 1fr) 330px;
      align-items: center;
      gap: clamp(42px, 7vw, 110px);
    }

    .hero-content {
      max-width: 760px;
      padding-top: 18px;
    }

    .hero-eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 15px;
      margin: 0 0 24px;
      color: var(--moss);
      font-family: var(--serif);
      font-size: 0.8rem;
      letter-spacing: 0.28em;
    }

    .hero-eyebrow::before {
      content: "";
      width: 42px;
      height: 1px;
      background: currentColor;
      opacity: 0.58;
    }

    .hero-title {
      margin: 0;
      font-family: var(--serif);
      font-size: clamp(4.2rem, 7.4vw, 6.8rem);
      font-weight: 500;
      line-height: 0.98;
      letter-spacing: 0.11em;
      text-wrap: balance;
    }

    .hero-title .accent-dot {
      color: var(--cinnabar);
      font-size: 0.42em;
      vertical-align: 0.14em;
      margin-left: 0.08em;
    }

    .hero-subtitle {
      margin: 28px 0 0;
      color: var(--ink-soft);
      font-family: var(--serif);
      font-size: clamp(1.12rem, 2vw, 1.48rem);
      letter-spacing: 0.12em;
    }

    .hero-intro {
      max-width: 580px;
      margin-top: 34px;
      padding-left: 22px;
      border-left: 1px solid rgba(164, 68, 52, 0.48);
      color: var(--ink-soft);
      font-size: 1rem;
      line-height: 2.05;
    }

    .hero-actions {
      display: flex;
      align-items: center;
      gap: 30px;
      margin-top: 42px;
    }

    .action-primary,
    .action-secondary {
      position: relative;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 44px;
      font-family: var(--serif);
      letter-spacing: 0.12em;
      transition: color 230ms ease, transform 230ms var(--ease), border-color 230ms ease;
    }

    .action-primary {
      min-width: 126px;
      padding: 8px 20px;
      color: var(--paper-light);
      background: var(--ink);
      border: 1px solid var(--ink);
    }

    .action-primary::after {
      content: "";
      position: absolute;
      inset: 4px;
      border: 1px solid rgba(255,255,255,0.2);
    }

    .action-primary:hover {
      transform: translateY(-2px);
      background: var(--moss-deep);
      border-color: var(--moss-deep);
    }

    .action-secondary {
      padding: 8px 0 6px;
      border-bottom: 1px solid var(--line-strong);
    }

    .action-secondary::after {
      content: "";
      position: absolute;
      left: 0;
      right: 0;
      bottom: -1px;
      height: 1px;
      background: var(--cinnabar);
      transform: scaleX(0);
      transform-origin: right;
      transition: transform 260ms var(--ease);
    }

    .action-secondary:hover::after {
      transform: scaleX(1);
      transform-origin: left;
    }

    .hero-side {
      position: relative;
      align-self: stretch;
      min-height: 350px;
      display: flex;
      align-items: center;
      justify-content: flex-end;
    }

    .hero-vertical-copy {
      position: absolute;
      right: 4px;
      top: 42px;
      display: flex;
      flex-direction: column;
      gap: 10px;
      color: rgba(48, 50, 46, 0.68);
      font-family: var(--serif);
      font-size: 0.9rem;
      letter-spacing: 0.28em;
      writing-mode: vertical-rl;
    }

    .hero-vertical-copy::before {
      content: "";
      width: 1px;
      height: 64px;
      margin: 0 auto 12px;
      background: linear-gradient(var(--moss), transparent);
      opacity: 0.38;
    }

    .hero-note {
      position: relative;
      width: 286px;
      margin-right: 46px;
      padding: 28px 28px 26px;
      color: var(--ink-soft);
      background: rgba(249, 247, 240, 0.72);
      border-top: 1px solid var(--line-strong);
      border-bottom: 1px solid var(--line);
    }

    .hero-note::before {
      content: "";
      position: absolute;
      left: -30px;
      top: 20px;
      width: 52px;
      height: 74px;
      background:
        radial-gradient(ellipse at 40% 55%, rgba(63, 72, 65, 0.22), transparent 58%),
        radial-gradient(ellipse at 70% 35%, rgba(63, 72, 65, 0.12), transparent 48%);
      filter: blur(3px);
      transform: rotate(-14deg);
    }

    .hero-note-head {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 18px;
      margin-bottom: 20px;
      color: var(--ink);
      font-family: var(--serif);
    }

    .hero-note-head strong {
      font-size: 1.05rem;
      font-weight: 500;
      letter-spacing: 0.18em;
    }

    .hero-note-head time {
      color: var(--ink-faint);
      font-size: 0.72rem;
      letter-spacing: 0.08em;
    }

    .hero-note dl,
    .hero-note dd {
      margin: 0;
    }

    .hero-note dl {
      display: grid;
      gap: 17px;
    }

    .hero-note dl > div {
      display: grid;
      grid-template-columns: 70px 1fr;
      gap: 12px;
      padding-top: 15px;
      border-top: 1px solid var(--line);
    }

    .hero-note dt {
      color: var(--moss);
      font-size: 0.74rem;
      letter-spacing: 0.08em;
    }

    .hero-note dd {
      font-family: var(--serif);
      font-size: 0.94rem;
      line-height: 1.75;
    }

    .hero-landscape {
      position: absolute;
      inset: 0;
      z-index: -4;
      pointer-events: none;
    }

    .hero-landscape img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center 56%;
    }

    .scroll-hint {
      position: absolute;
      left: 50%;
      bottom: 28px;
      z-index: 5;
      display: grid;
      justify-items: center;
      gap: 8px;
      color: var(--ink-faint);
      font-family: var(--serif);
      font-size: 0.72rem;
      letter-spacing: 0.25em;
      transform: translateX(-50%);
    }

    .scroll-hint::after {
      content: "";
      width: 1px;
      height: 30px;
      background: linear-gradient(var(--ink-faint), transparent);
    }

    /* =========================
       书斋目录
    ========================== */
    .study-index {
      background:
        linear-gradient(90deg, rgba(73, 81, 74, 0.025), transparent 30%, transparent 70%, rgba(73, 81, 74, 0.025)),
        rgba(250, 248, 242, 0.45);
      border-top: 1px solid rgba(82, 78, 69, 0.08);
      border-bottom: 1px solid rgba(82, 78, 69, 0.08);
    }

    .study-grid {
      display: grid;
      grid-template-columns: 330px minmax(0, 1fr);
      gap: clamp(56px, 8vw, 112px);
      align-items: start;
    }

    .study-intro {
      position: sticky;
      top: 118px;
      padding-top: 12px;
    }

    .study-title {
      margin: 0;
      font-family: var(--serif);
      font-size: clamp(2.35rem, 4.7vw, 4rem);
      font-weight: 500;
      line-height: 1.15;
      letter-spacing: 0.1em;
    }

    .study-intro > p:last-of-type {
      margin: 26px 0 0;
      color: var(--ink-soft);
      font-family: var(--serif);
      line-height: 2;
    }

    .study-mini-painting {
      position: relative;
      width: 100%;
      height: 190px;
      margin-top: 34px;
      overflow: hidden;
      border-top: 1px solid var(--line);
    }

    .study-mini-painting svg {
      width: 100%;
      height: 100%;
    }

    .index-list {
      border-top: 1px solid var(--line-strong);
    }

    .index-item {
      position: relative;
      display: grid;
      grid-template-columns: 60px minmax(150px, 0.48fr) minmax(220px, 1fr) 70px;
      align-items: center;
      gap: 24px;
      min-height: 118px;
      border-bottom: 1px solid var(--line);
      transition: color 240ms ease, transform 260ms var(--ease), background-color 240ms ease;
    }

    .index-item::before {
      content: "";
      position: absolute;
      left: 0;
      top: 50%;
      width: 0;
      height: 1px;
      background: var(--cinnabar);
      transition: width 320ms var(--ease);
    }

    .index-item:hover {
      color: var(--moss-deep);
      background: linear-gradient(90deg, rgba(164, 68, 52, 0.035), transparent 42%);
      transform: translateX(6px);
    }

    .index-item:hover::before {
      width: 28px;
    }

    .index-number {
      color: var(--cinnabar);
      font-family: var(--serif);
      font-size: 0.88rem;
      letter-spacing: 0.12em;
      transition: transform 260ms var(--ease);
    }

    .index-item:hover .index-number {
      transform: translateX(32px);
    }

    .index-name {
      font-family: var(--serif);
      font-size: clamp(1.28rem, 2vw, 1.7rem);
      letter-spacing: 0.08em;
    }

    .index-desc,
    .index-count {
      color: var(--ink-soft);
      font-size: 0.88rem;
    }

    .index-count {
      text-align: right;
      font-family: var(--serif);
    }

    /* =========================
       最新文章：水墨横卷
    ========================== */
    .latest {
      background:
        radial-gradient(ellipse at 100% 22%, rgba(73, 82, 74, 0.08), transparent 27%),
        radial-gradient(ellipse at 0% 76%, rgba(73, 82, 74, 0.055), transparent 28%),
        rgba(250, 248, 242, 0.72);
    }

    .scroll-list {
      position: relative;
      scroll-margin-top: 76px;
      display: grid;
      gap: clamp(24px, 2.4vw, 30px);
    }

    .scroll-list::before {
      content: "";
      position: absolute;
      left: 5%;
      right: 5%;
      top: -22px;
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--line-strong), transparent);
    }

    .article-scroll {
      --scroll-motion: 520ms cubic-bezier(0.22, 1, 0.36, 1);
      position: relative;
      isolation: isolate;
      display: grid;
      grid-template-columns: 12px minmax(0, 1fr) 12px;
      height: 180px;
      min-height: 0;
      filter: drop-shadow(0 10px 18px rgba(63, 54, 41, 0.09));
      transition: filter var(--scroll-motion);
    }

    .article-scroll:nth-child(2) {
      margin-left: clamp(0px, 4vw, 52px);
      margin-right: clamp(0px, 1vw, 12px);
    }

    .article-scroll:nth-child(3) {
      margin-left: clamp(0px, 1vw, 14px);
      margin-right: clamp(0px, 4vw, 54px);
    }

    .article-scroll:hover,
    .article-scroll:focus-within {
      filter: drop-shadow(0 14px 24px rgba(63, 54, 41, 0.12));
    }

    .scroll-axis {
      position: relative;
      z-index: 2;
      margin: -3px 0;
      border-radius: 6px;
      background:
        repeating-linear-gradient(180deg, transparent 0 17px, rgba(58, 42, 30, 0.05) 18px, transparent 19px),
        linear-gradient(90deg, #57453a, #79614e 30%, #8a7060 50%, #6d584a 72%, #524239);
      border: 1px solid rgba(72, 58, 46, 0.22);
      transform: translateX(7px);
      box-shadow:
        inset 2px 0 4px rgba(255,255,255,0.1),
        inset -2px 0 4px rgba(30, 24, 19, 0.1),
        0 4px 8px rgba(45, 38, 31, 0.08);
      transition: transform var(--scroll-motion);
      will-change: transform;
    }

    .scroll-axis:last-child {
      transform: translateX(-7px);
    }

    .article-scroll:hover .scroll-axis,
    .article-scroll:focus-within .scroll-axis {
      transform: translateX(0);
    }

    .scroll-axis::before,
    .scroll-axis::after {
      content: "";
      position: absolute;
      left: 50%;
      width: 16px;
      height: 6px;
      border-radius: 50%;
      background:
        radial-gradient(ellipse at 48% 30%, rgba(255,255,255,0.12), transparent 40%),
        linear-gradient(180deg, #7d6852, #5a4a3c 78%);
      border: 1px solid rgba(72, 58, 46, 0.26);
      transform: translateX(-50%);
      box-shadow: inset 0 1px 2px rgba(255,255,255,0.1);
    }

    .scroll-axis::before { top: -3px; }
    .scroll-axis::after { bottom: -3px; }

    .scroll-paper {
      position: relative;
      z-index: 3;
      min-width: 0;
      height: 100%;
      display: grid;
      grid-template-columns: 96px minmax(0, 1fr);
      align-items: center;
      gap: 6px 18px;
      padding: 20px 26px 16px;
      overflow: hidden;
      clip-path: polygon(6px 1%, 18% 0, 41% 1.1%, 64% 0.2%, 84% 1.2%, calc(100% - 6px) 0.5%, calc(100% - 5px) 99%, 78% 98.7%, 55% 100%, 31% 98.8%, 7px 100%);
      transform: scaleX(0.985);
      transform-origin: center;
      background-color: #f1ead9;
      background-image:
        url("data:image/svg+xml,%3Csvg viewBox='0 0 220 220' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='f'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.52' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23f)' opacity='.15'/%3E%3C/svg%3E"),
        radial-gradient(ellipse at 20% 22%, rgba(123, 96, 63, 0.05), transparent 32%),
        radial-gradient(ellipse at 50% 42%, rgba(255, 252, 244, 0.62), transparent 62%),
        linear-gradient(180deg, transparent 0 6px, rgba(122, 94, 58, 0.13) 6px 7px, transparent 7px calc(100% - 7px), rgba(122, 94, 58, 0.11) calc(100% - 7px) calc(100% - 6px), transparent calc(100% - 6px)),
        linear-gradient(180deg, rgba(255,255,255,0.22), transparent 22%, transparent 78%, rgba(88, 65, 39, 0.07)),
        linear-gradient(90deg, rgba(112, 80, 44, 0.15), transparent 8%, transparent 92%, rgba(112, 80, 44, 0.14));
      background-size: 220px 220px, auto, auto, auto, auto, auto;
      background-blend-mode: soft-light, normal, normal, normal, normal, normal;
      border-top: 1px solid rgba(118, 97, 68, 0.16);
      border-bottom: 1px solid rgba(118, 97, 68, 0.14);
      box-shadow:
        inset 22px 0 22px -8px rgba(99, 71, 40, 0.1),
        inset -22px 0 22px -8px rgba(99, 71, 40, 0.09),
        inset 0 8px 12px rgba(255,255,255,0.16),
        inset 0 -8px 13px rgba(95, 71, 45, 0.07);
      transition: transform var(--scroll-motion), clip-path var(--scroll-motion), box-shadow var(--scroll-motion);
      will-change: transform, clip-path;
    }

    .article-scroll:hover .scroll-paper,
    .article-scroll:focus-within .scroll-paper {
      clip-path: polygon(2px 1%, 18% 0, 41% 1.1%, 64% 0.2%, 84% 1.2%, calc(100% - 2px) 0.5%, calc(100% - 2px) 99%, 78% 98.7%, 55% 100%, 31% 98.8%, 2px 100%);
      transform: scaleX(1);
      box-shadow:
        inset 16px 0 18px -8px rgba(99, 71, 40, 0.06),
        inset -16px 0 18px -8px rgba(99, 71, 40, 0.055),
        inset 0 7px 11px rgba(255,255,255,0.14),
        inset 0 -7px 12px rgba(95, 71, 45, 0.05);
    }

    .scroll-paper::before,
    .scroll-paper::after {
      content: "";
      position: absolute;
      top: 2px;
      bottom: 2px;
      z-index: 2;
      width: 30px;
      pointer-events: none;
      opacity: 0.94;
      transition: transform var(--scroll-motion), opacity var(--scroll-motion), box-shadow var(--scroll-motion);
    }

    .scroll-paper::before {
      left: 0;
      transform-origin: left center;
      background:
        repeating-linear-gradient(90deg, transparent 0 7px, rgba(96, 70, 42, 0.05) 7px 8px),
        linear-gradient(90deg, rgba(72, 52, 32, 0.26), rgba(214, 197, 170, 0.95) 18%, rgba(255, 251, 240, 0.95) 38%, rgba(238, 226, 203, 0.9) 55%, rgba(148, 113, 71, 0.2) 76%, rgba(148, 113, 71, 0.06) 88%, transparent);
      border-radius: 42% 20% 20% 42% / 4% 4% 96% 96%;
      box-shadow: inset 6px 0 8px -2px rgba(80, 58, 35, 0.16), inset -4px 0 6px -2px rgba(255, 252, 244, 0.35), 6px 0 10px -2px rgba(86, 61, 36, 0.1);
    }

    .scroll-paper::after {
      right: 0;
      transform-origin: right center;
      background:
        repeating-linear-gradient(90deg, transparent 0 7px, rgba(96, 70, 42, 0.05) 7px 8px),
        linear-gradient(270deg, rgba(72, 52, 32, 0.26), rgba(214, 197, 170, 0.95) 18%, rgba(255, 251, 240, 0.95) 38%, rgba(238, 226, 203, 0.9) 55%, rgba(148, 113, 71, 0.2) 76%, rgba(148, 113, 71, 0.06) 88%, transparent);
      border-radius: 20% 42% 42% 20% / 4% 4% 96% 96%;
      box-shadow: inset -6px 0 8px -2px rgba(80, 58, 35, 0.16), inset 4px 0 6px -2px rgba(255, 252, 244, 0.35), -6px 0 10px -2px rgba(86, 61, 36, 0.1);
    }

    .article-scroll:hover .scroll-paper::before,
    .article-scroll:focus-within .scroll-paper::before,
    .article-scroll:hover .scroll-paper::after,
    .article-scroll:focus-within .scroll-paper::after {
      transform: scaleX(0.66);
      opacity: 0.76;
      box-shadow: inset 3px 0 5px rgba(102, 72, 41, 0.08);
    }

    .scroll-paper > *:not(.scroll-ink) {
      position: relative;
      z-index: 3;
    }

    .scroll-ink {
      position: absolute;
      inset: 0;
      z-index: 1;
      pointer-events: none;
      opacity: 0.42;
    }

    .scroll-ink svg {
      width: 100%;
      height: 100%;
    }

    .scroll-seal {
      position: absolute !important;
      right: 15px;
      top: 11px;
      width: 26px;
      aspect-ratio: 1;
      display: grid;
      place-items: center;
      color: rgba(249, 237, 226, 0.94);
      background: rgba(154, 65, 52, 0.78);
      border: 1px solid rgba(127, 48, 38, 0.5);
      font-family: var(--serif);
      font-size: 0.54rem;
      line-height: 1.2;
      text-align: center;
      clip-path: polygon(4% 2%, 96% 0, 100% 94%, 92% 100%, 3% 96%, 0 8%);
      transform: rotate(-2deg);
      mix-blend-mode: multiply;
      box-shadow: inset 0 0 0 1px rgba(255,255,255,0.08);
    }

    .post-meta {
      grid-row: 1 / span 2;
      align-self: stretch;
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 2px;
      padding-right: 15px;
      color: var(--ink-soft);
      font-size: 0.72rem;
    }

    .post-meta::after {
      content: "";
      position: absolute;
      top: 2px;
      right: 0;
      bottom: 2px;
      width: 1px;
      background: linear-gradient(180deg, transparent, rgba(79, 59, 40, 0.16) 18%, rgba(79, 59, 40, 0.16) 82%, transparent);
    }

    .post-meta::before {
      content: attr(data-index);
      display: block;
      margin-bottom: 2px;
      color: var(--cinnabar);
      font-family: var(--serif);
      font-size: 0.92rem;
      letter-spacing: 0.16em;
    }

    .post-meta time {
      font-family: var(--serif);
      letter-spacing: 0.04em;
    }

    .post-meta .category {
      margin-top: 3px;
      color: var(--moss-deep);
      font-family: var(--serif);
      font-size: 0.82rem;
      letter-spacing: 0.08em;
    }

    .reading-time {
      color: var(--ink-faint);
    }

    .post-main {
      min-width: 0;
      padding-right: 30px;
      opacity: 0.92;
      transform: translateY(2px);
      transition: opacity var(--scroll-motion), transform var(--scroll-motion);
    }

    .post-title {
      margin: 0;
      font-family: var(--serif);
      display: -webkit-box;
      overflow: hidden;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 2;
      font-size: clamp(1.48rem, 2vw, 1.75rem);
      font-weight: 500;
      line-height: 1.3;
      letter-spacing: 0.045em;
      text-wrap: balance;
    }

    .post-title a {
      background-image: linear-gradient(var(--cinnabar), var(--cinnabar));
      background-repeat: no-repeat;
      background-position: left 100%;
      background-size: 0 1px;
      transition: color var(--scroll-motion), background-size var(--scroll-motion);
    }

    .article-scroll:hover .post-title a,
    .article-scroll:focus-within .post-title a {
      color: #1f2420;
      background-size: 100% 1px;
    }

    .post-excerpt {
      max-width: 650px;
      margin: 10px 0 0;
      display: -webkit-box;
      overflow: hidden;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 2;
      color: var(--ink-soft);
      font-family: var(--serif);
      font-size: 0.84rem;
      line-height: 1.6;
    }

    .post-tags {
      grid-column: 2;
      align-self: start;
      display: flex;
      flex-wrap: nowrap;
      gap: 0 10px;
      min-width: 0;
      overflow: hidden;
      white-space: nowrap;
      color: var(--ink-soft);
      font-family: var(--serif);
      font-size: 0.72rem;
      letter-spacing: 0.08em;
      opacity: 0.92;
      transform: translateY(2px);
      transition: opacity var(--scroll-motion), transform var(--scroll-motion);
    }

    .post-tags::before {
      content: "题签";
      flex: 0 0 auto;
      margin-right: 4px;
      color: var(--cinnabar);
      font-size: 0.64rem;
      letter-spacing: 0.18em;
    }

    .article-scroll:hover .post-main,
    .article-scroll:hover .post-tags,
    .article-scroll:focus-within .post-main,
    .article-scroll:focus-within .post-tags {
      opacity: 1;
      transform: translateY(0);
    }

    .post-tags span::before {
      content: "「";
      color: rgba(164, 68, 52, 0.62);
    }

    .post-tags span::after {
      content: "」";
      color: rgba(164, 68, 52, 0.62);
    }

    .all-posts {
      display: flex;
      justify-content: flex-end;
      margin-top: 42px;
    }

    .all-posts a {
      position: relative;
      padding: 8px 30px 8px 0;
      color: var(--ink-soft);
      font-family: var(--serif);
      letter-spacing: 0.12em;
    }

    .all-posts a::before {
      content: "";
      position: absolute;
      right: 0;
      top: 50%;
      width: 20px;
      height: 1px;
      background: var(--cinnabar);
      transition: width 260ms var(--ease);
    }

    .all-posts a:hover::before {
      width: 28px;
    }

    /* =========================
       案头近况：墨色过渡区
    ========================== */
    .desk {
      position: relative;
      color: rgba(248, 246, 239, 0.92);
      background:
        radial-gradient(ellipse at 18% 38%, rgba(255,255,255,0.045), transparent 33%),
        radial-gradient(ellipse at 80% 12%, rgba(164,68,52,0.08), transparent 22%),
        linear-gradient(135deg, #3d4540, #303531 60%, #3a403b);
      overflow: hidden;
    }

    .desk::before {
      content: "";
      position: absolute;
      inset: 0;
      pointer-events: none;
      opacity: 0.22;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 220 220' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='d'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.46' numOctaves='4'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23d)' opacity='.6'/%3E%3C/svg%3E");
      mix-blend-mode: soft-light;
    }

    .desk::after {
      content: "";
      position: absolute;
      right: -7%;
      bottom: -22%;
      width: min(50vw, 760px);
      aspect-ratio: 1.4;
      border-radius: 50%;
      border: 1px solid rgba(235, 232, 222, 0.08);
      box-shadow:
        0 0 0 34px rgba(235,232,222,0.025),
        0 0 0 82px rgba(235,232,222,0.015);
      transform: rotate(-8deg);
    }

    .desk .section-kicker {
      color: #ce8b7e;
    }

    .desk .section-title {
      color: #f1eee6;
    }

    .desk .section-note {
      color: rgba(242, 239, 230, 0.64);
    }

    .desk-grid {
      position: relative;
      z-index: 2;
      display: grid;
      grid-template-columns: minmax(0, 1.05fr) minmax(360px, 0.8fr);
      gap: clamp(32px, 5vw, 70px);
    }

    .now-panel,
    .fragment-panel {
      position: relative;
      min-width: 0;
      border-top: 1px solid rgba(240, 236, 226, 0.28);
      border-bottom: 1px solid rgba(240, 236, 226, 0.12);
      background: rgba(255,255,255,0.025);
    }

    .now-panel {
      padding: 30px 34px 18px;
    }

    .panel-mark {
      position: absolute;
      right: 26px;
      top: -20px;
      width: 42px;
      aspect-ratio: 1;
      display: grid;
      place-items: center;
      color: rgba(248, 238, 229, 0.94);
      background: var(--cinnabar);
      border: 1px solid rgba(118, 42, 33, 0.8);
      font-family: var(--serif);
      transform: rotate(4deg);
    }

    .now-head {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 20px;
      padding-bottom: 20px;
      color: rgba(247,244,235,0.9);
      font-family: var(--serif);
    }

    .now-head span {
      font-size: 1.1rem;
      letter-spacing: 0.15em;
    }

    .now-head time {
      color: rgba(247,244,235,0.45);
      font-size: 0.78rem;
    }

    .now-list {
      border-top: 1px solid rgba(240, 236, 226, 0.12);
    }

    .now-row {
      display: grid;
      grid-template-columns: 78px minmax(0, 1fr) 64px;
      gap: 20px;
      align-items: center;
      min-height: 92px;
      border-bottom: 1px solid rgba(240, 236, 226, 0.11);
    }

    .now-label {
      color: #d79a8d;
      font-size: 0.78rem;
      letter-spacing: 0.08em;
    }

    .now-row strong {
      color: rgba(248,246,239,0.88);
      font-family: var(--serif);
      font-size: 1.02rem;
      font-weight: 400;
      line-height: 1.7;
    }

    .now-state {
      justify-self: end;
      color: rgba(248,246,239,0.43);
      font-size: 0.74rem;
    }

    .fragment-panel {
      display: grid;
      grid-template-rows: 210px 1fr;
      overflow: hidden;
    }

    .fragment-art {
      position: relative;
      overflow: hidden;
      border-bottom: 1px solid rgba(240,236,226,0.12);
    }

    .fragment-art svg {
      width: 100%;
      height: 100%;
    }

    .fragment-copy {
      padding: 26px 30px 30px;
    }

    .fragment-label {
      margin: 0 0 12px;
      color: #d79a8d;
      font-family: var(--serif);
      font-size: 0.78rem;
      letter-spacing: 0.18em;
    }

    .fragment-copy blockquote {
      margin: 0;
      color: rgba(248,246,239,0.88);
      font-family: var(--serif);
      font-size: clamp(1.1rem, 1.8vw, 1.35rem);
      line-height: 1.9;
    }

    .fragment-source {
      margin: 18px 0 0;
      color: rgba(248,246,239,0.4);
      font-size: 0.78rem;
      text-align: right;
    }

    /* =========================
       关于此间
    ========================== */
    .about {
      overflow: hidden;
      background:
        linear-gradient(180deg, rgba(255,255,255,0.28), transparent 48%),
        var(--paper);
    }

    .about-grid {
      display: grid;
      grid-template-columns: minmax(0, 0.85fr) minmax(380px, 0.72fr);
      gap: clamp(54px, 9vw, 130px);
      align-items: center;
    }

    .about-text {
      max-width: 650px;
      margin-top: 34px;
      color: var(--ink-soft);
      font-family: var(--serif);
      font-size: clamp(1rem, 1.4vw, 1.15rem);
      line-height: 2.15;
    }

    .about-text p {
      margin: 0 0 14px;
    }

    .about-signature {
      display: inline-block;
      margin-top: 28px;
      padding: 7px 0;
      border-bottom: 1px solid rgba(164,68,52,0.45);
      color: var(--moss-deep);
      font-family: var(--serif);
      letter-spacing: 0.16em;
    }

    .about-topics {
      display: flex;
      flex-wrap: wrap;
      gap: 18px 28px;
      margin-top: 34px;
      color: var(--ink-faint);
      font-family: var(--serif);
      font-size: 0.88rem;
    }

    .about-topics span::before {
      content: "·";
      margin-right: 8px;
      color: var(--cinnabar);
    }

    .ink-orbit {
      position: relative;
      min-height: 510px;
      display: grid;
      place-items: center;
    }

    .ink-circle {
      position: absolute;
      width: min(38vw, 430px);
      aspect-ratio: 1;
      border-radius: 50%;
      border: 12px solid transparent;
      background:
        radial-gradient(circle at 51% 49%, var(--paper) 0 57%, transparent 58%),
        conic-gradient(from 12deg,
          rgba(44,48,44,0.82),
          rgba(63,70,64,0.4) 12%,
          rgba(44,48,44,0.78) 22%,
          rgba(63,70,64,0.22) 36%,
          rgba(44,48,44,0.72) 52%,
          rgba(63,70,64,0.28) 66%,
          rgba(44,48,44,0.84) 82%,
          rgba(63,70,64,0.3));
      filter: blur(0.35px);
      opacity: 0.72;
      transform: rotate(-12deg) scaleX(1.02);
      -webkit-mask: radial-gradient(circle, transparent 0 56%, #000 57% 66%, transparent 67%);
      mask: radial-gradient(circle, transparent 0 56%, #000 57% 66%, transparent 67%);
    }

    .ink-circle::before,
    .ink-circle::after {
      content: "";
      position: absolute;
      border-radius: 50%;
      background: rgba(46, 51, 47, 0.16);
      filter: blur(4px);
    }

    .ink-circle::before {
      width: 42px;
      height: 20px;
      left: -6px;
      top: 52%;
      transform: rotate(-20deg);
    }

    .ink-circle::after {
      width: 30px;
      height: 14px;
      right: 8px;
      bottom: 20%;
      transform: rotate(18deg);
    }

    .bamboo {
      position: relative;
      z-index: 2;
      width: min(31vw, 350px);
      overflow: visible;
    }

    .about-seal {
      position: absolute;
      z-index: 4;
      right: 11%;
      bottom: 13%;
      width: 58px;
      aspect-ratio: 1;
      display: grid;
      place-items: center;
      color: #f8eee4;
      background: rgba(164,68,52,0.9);
      border: 1px solid var(--cinnabar-deep);
      font-family: var(--serif);
      font-size: 0.72rem;
      line-height: 1.45;
      text-align: center;
      transform: rotate(4deg);
      box-shadow: inset 0 0 0 4px rgba(255,255,255,0.13);
    }

    /* =========================
       动画
    ========================== */
    .hero-content {
      animation: hero-in 300ms var(--ease) both;
    }

    .hero-note {
      animation: note-in 300ms 40ms var(--ease) both;
    }

    .reveal {
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 300ms ease, transform 300ms var(--ease);
    }

    .reveal.is-visible {
      opacity: 1;
      transform: translateY(0);
    }

    @keyframes hero-in {
      from { opacity: 0; transform: translateY(24px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes note-in {
      from { opacity: 0; transform: translateX(20px); }
      to { opacity: 1; transform: translateX(0); }
    }

    /* 借鉴 Firefly 的侧栏骨架，按宽度渐进重排现有内容。 */
    @media (min-width: 821px) {
      .home-layout {
        position: relative;
        z-index: 6;
        width: min(calc(100% - (var(--side-space) * 2)), 1220px);
        margin: 24px auto 0;
        display: grid;
        align-items: start;
        gap: 24px;
      }

      .home-layout > .study-index { grid-area: study; }
      .home-layout > .latest { grid-area: latest; }
      .home-layout > .desk { grid-area: desk; }
      .home-layout > .about { grid-area: about; }

      .home-layout > .section {
        min-width: 0;
        padding: 54px 0;
        border-radius: 16px;
      }

      .home-layout .shell {
        width: calc(100% - 48px);
      }

      .home-layout .study-grid,
      .home-layout .about-grid {
        grid-template-columns: 1fr;
      }

      .home-layout .latest .section-heading,
      .home-layout .about .section-heading {
        grid-template-columns: 1fr;
        gap: 18px;
        margin-bottom: 38px;
      }

      .home-layout .study-grid,
      .home-layout .about-grid {
        gap: 30px;
      }

      .home-layout .study-intro {
        position: static;
        padding-top: 0;
      }

      .home-layout .study-title,
      .home-layout .about .section-title {
        font-size: clamp(2rem, 2.8vw, 2.65rem);
      }

      .home-layout .index-item {
        grid-template-columns: 30px minmax(0, 1fr) auto;
        gap: 8px 12px;
        min-height: 94px;
        padding: 16px 0;
      }

      .home-layout .index-desc {
        grid-column: 1 / -1;
        padding-left: 42px;
      }

      .home-layout .index-count {
        grid-column: 3;
        grid-row: 1;
      }

      .home-layout .brush-divider {
        width: 100%;
      }

      .home-layout .latest .article-scroll,
      .home-layout .latest .article-scroll:nth-child(2),
      .home-layout .latest .article-scroll:nth-child(3) {
        margin-inline: 0;
      }

      .home-layout .latest .scroll-paper {
        grid-template-columns: 92px minmax(0, 1fr);
        gap: 6px 18px;
        padding: 20px 26px 16px;
      }

      .home-layout .latest .post-tags {
        grid-column: 2;
        justify-content: flex-start;
      }

      .home-layout .latest .post-tags::before {
        width: auto;
      }

      .home-layout .about .ink-orbit {
        min-height: 300px;
      }

      .home-layout .about .ink-circle {
        width: min(100%, 240px);
      }

      .home-layout .about .bamboo {
        width: min(82%, 210px);
      }
    }

    @media (min-width: 821px) and (max-width: 1359px) {
      .home-layout {
        grid-template-columns: clamp(210px, 22vw, 280px) minmax(0, 1fr);
        grid-template-areas:
          "study latest"
          "about latest"
          "desk desk";
      }
    }

    @media (min-width: 1360px) {
      .home-layout {
        width: min(calc(100% - (var(--side-space) * 2)), 1480px);
        grid-template-columns: clamp(240px, 18vw, 280px) minmax(620px, 1fr) clamp(260px, 19vw, 300px);
        grid-template-areas:
          "study latest desk"
          "about latest desk";
      }

      .home-layout .desk .section-heading,
      .home-layout .desk-grid {
        grid-template-columns: 1fr;
      }

      .home-layout .desk .section-heading {
        gap: 16px;
        margin-bottom: 30px;
      }

      .home-layout .desk-grid {
        gap: 24px;
      }

      .home-layout .desk .section-title {
        font-size: clamp(2rem, 2.8vw, 2.65rem);
      }

      .home-layout .desk .now-panel {
        padding: 24px 20px 14px;
      }

      .home-layout .desk .now-row {
        grid-template-columns: 1fr;
        gap: 5px;
        padding: 15px 0;
      }

      .home-layout .desk .now-state {
        justify-self: start;
      }

      .home-layout .desk .fragment-panel {
        grid-template-rows: 140px 1fr;
      }

      .home-layout .desk .fragment-copy {
        padding: 20px;
      }
    }

    /* =========================
       响应式布局
    ========================== */
    @media (max-width: 1080px) {
      .hero-grid {
        grid-template-columns: minmax(0, 1fr) 270px;
        gap: 48px;
      }

      .hero-note {
        width: 250px;
        margin-right: 32px;
        padding: 24px;
      }

      .study-grid {
        grid-template-columns: 270px minmax(0, 1fr);
        gap: 56px;
      }

      .index-item {
        grid-template-columns: 46px minmax(130px, 0.55fr) minmax(170px, 1fr) 60px;
        gap: 18px;
      }

      .desk-grid {
        grid-template-columns: 1fr;
      }

      .fragment-panel {
        grid-template-columns: minmax(300px, 0.85fr) minmax(0, 1fr);
        grid-template-rows: auto;
      }

      .fragment-art {
        min-height: 300px;
        border-right: 1px solid rgba(240,236,226,0.12);
        border-bottom: 0;
      }
    }

    @media (max-width: 820px) {
      .section-heading {
        grid-template-columns: 1fr;
        align-items: start;
        gap: 22px;
      }

      .section-note {
        max-width: 620px;
      }

      .hero {
        min-height: 620px;
        height: auto;
        padding: 126px 0 82px;
      }

      .hero-grid {
        grid-template-columns: 1fr;
      }

      .hero-content {
        max-width: 680px;
      }

      .hero-side {
        display: none;
      }

      .hero-vertical-copy {
        display: none;
      }

      .hero-note {
        width: min(100%, 430px);
        margin: 10px 0 0 auto;
      }

      .hero-landscape {
        height: 100%;
      }

      .study-grid {
        grid-template-columns: 1fr;
      }

      .study-intro {
        position: static;
        display: grid;
        grid-template-columns: minmax(0, 1fr) 260px;
        gap: 36px;
      }

      .study-mini-painting {
        height: 180px;
        margin-top: 0;
      }

      .index-item {
        grid-template-columns: 42px minmax(150px, 0.7fr) minmax(180px, 1fr) 58px;
      }

      .article-scroll,
      .article-scroll:nth-child(2),
      .article-scroll:nth-child(3) {
        margin-inline: 0;
      }

      .scroll-paper {
        grid-template-columns: 96px minmax(0, 1fr);
        gap: 6px 18px;
        padding: 20px 26px 16px;
      }

      .post-tags {
        grid-column: 2;
        justify-content: flex-start;
      }

      .post-tags::before {
        width: auto;
      }

      .about-grid {
        grid-template-columns: 1fr;
      }

      .ink-orbit {
        min-height: 420px;
      }

      .ink-circle {
        width: min(62vw, 390px);
      }

      .bamboo {
        width: min(48vw, 320px);
      }
    }

    @media (max-width: 620px) {
      .hero {
        min-height: 570px;
        padding: 104px 0 62px;
      }

      .hero-title {
        font-size: clamp(3.4rem, 18vw, 5.4rem);
        letter-spacing: 0.07em;
      }

      .hero-subtitle {
        margin-top: 22px;
        font-size: 1rem;
        letter-spacing: 0.08em;
      }

      .hero-intro {
        margin-top: 26px;
        font-size: 0.92rem;
      }

      .hero-actions {
        gap: 22px;
        margin-top: 32px;
      }

      .action-primary {
        min-width: 112px;
        padding-inline: 16px;
      }

      .hero-note {
        padding: 22px 20px;
      }

      .hero-note dl > div {
        grid-template-columns: 62px 1fr;
      }

      .hero-landscape {
        height: 100%;
        min-height: 0;
      }

      .scroll-hint {
        display: none;
      }

      .section {
        padding: 82px 0;
      }

      .section-title,
      .study-title {
        font-size: clamp(2.2rem, 12vw, 3.2rem);
      }

      .study-intro {
        display: block;
      }

      .study-mini-painting {
        height: 150px;
        margin-top: 30px;
      }

      .index-item {
        grid-template-columns: 30px 1fr auto;
        gap: 10px 14px;
        min-height: 102px;
        padding: 18px 0;
      }

      .index-name {
        font-size: 1.28rem;
      }

      .index-desc {
        grid-column: 2 / 4;
        font-size: 0.8rem;
      }

      .index-count {
        grid-column: 3;
        grid-row: 1;
      }

      .index-item:hover {
        transform: none;
      }

      .index-item:hover .index-number {
        transform: none;
      }

      .article-scroll {
        grid-template-columns: 10px minmax(0, 1fr) 10px;
        height: auto;
        min-height: 0;
      }

      .scroll-axis {
        margin: -3px 0;
        transform: none;
      }

      .scroll-axis:last-child {
        transform: none;
      }

      .scroll-axis::before,
      .scroll-axis::after {
        width: 16px;
        height: 6px;
      }

      .scroll-axis::before { top: -3px; }
      .scroll-axis::after { bottom: -3px; }

      .scroll-paper {
        height: auto;
        grid-template-columns: 1fr;
        gap: 14px;
        padding: 38px 20px 24px;
        clip-path: polygon(2px 1%, 18% 0, 41% 1.1%, 64% 0.2%, 84% 1.2%, calc(100% - 2px) 0.5%, calc(100% - 2px) 99%, 78% 98.7%, 55% 100%, 31% 98.8%, 2px 100%);
        transform: none;
      }

      .scroll-paper::before,
      .scroll-paper::after {
        top: 2px;
        bottom: 2px;
        width: 20px;
      }

      .scroll-seal {
        right: 14px;
        top: 12px;
        width: 25px;
        font-size: 0.52rem;
      }

      .post-meta {
        grid-row: auto;
        display: grid;
        grid-template-columns: minmax(0, 1fr) auto auto;
        gap: 4px 8px;
        padding: 0 0 12px;
      }

      .post-meta::after {
        top: auto;
        left: 0;
        right: 0;
        bottom: 0;
        width: auto;
        height: 1px;
        background: linear-gradient(90deg, transparent, rgba(79, 59, 40, 0.16) 14%, rgba(79, 59, 40, 0.16) 86%, transparent);
      }

      .post-meta::before {
        grid-column: 1 / -1;
        margin-bottom: 0;
      }

      .post-meta .category {
        margin-top: 0;
      }

      .reading-time {
        text-align: right;
      }

      .post-main {
        padding-right: 0;
        opacity: 1;
        transform: none;
      }

      .post-title {
        font-size: clamp(1.45rem, 7vw, 1.85rem);
      }

      .post-excerpt {
        margin-top: 10px;
        font-size: 0.88rem;
        line-height: 1.7;
      }

      .post-tags {
        grid-column: 1;
        gap: 0 8px;
        justify-content: flex-start;
        font-size: 0.74rem;
        opacity: 1;
        transform: none;
      }

      .post-tags::before {
        margin: 0 4px 0 0;
      }

      .now-panel {
        padding: 26px 18px 12px;
      }

      .now-row {
        grid-template-columns: 64px minmax(0, 1fr);
        gap: 12px;
        padding: 18px 0;
      }

      .now-state {
        grid-column: 2;
        justify-self: start;
      }

      .fragment-panel {
        grid-template-columns: 1fr;
      }

      .fragment-art {
        min-height: 220px;
        border-right: 0;
        border-bottom: 1px solid rgba(240,236,226,0.12);
      }

      .fragment-copy {
        padding: 24px 20px 28px;
      }

      .ink-orbit {
        min-height: 340px;
      }

      .ink-circle {
        width: min(84vw, 340px);
      }

      .bamboo {
        width: min(65vw, 280px);
      }

      .about-seal {
        right: 8%;
        bottom: 8%;
        width: 50px;
      }

    }

    @media (hover: none) {
      .article-scroll .scroll-axis,
      .article-scroll .scroll-axis:last-child {
        transform: none;
      }

      .article-scroll .scroll-paper {
        clip-path: polygon(2px 1%, 18% 0, 41% 1.1%, 64% 0.2%, 84% 1.2%, calc(100% - 2px) 0.5%, calc(100% - 2px) 99%, 78% 98.7%, 55% 100%, 31% 98.8%, 2px 100%);
        transform: none;
      }

      .article-scroll .scroll-paper::before,
      .article-scroll .scroll-paper::after {
        transform: scaleX(0.66);
        opacity: 0.76;
      }

      .article-scroll .post-main,
      .article-scroll .post-tags {
        opacity: 1;
        transform: none;
      }
    }

    @media (prefers-reduced-motion: reduce) {
      .reveal {
        opacity: 1;
        transform: none;
      }

      .article-scroll,
      .article-scroll .scroll-axis,
      .article-scroll .scroll-paper,
      .article-scroll .scroll-paper::before,
      .article-scroll .scroll-paper::after,
      .article-scroll .post-main,
      .article-scroll .post-tags,
      .article-scroll .post-title a {
        transition: none;
      }

      .article-scroll .scroll-axis,
      .article-scroll .scroll-axis:last-child {
        transform: none;
      }

      .article-scroll .scroll-paper {
        clip-path: polygon(2px 1%, 18% 0, 41% 1.1%, 64% 0.2%, 84% 1.2%, calc(100% - 2px) 0.5%, calc(100% - 2px) 99%, 78% 98.7%, 55% 100%, 31% 98.8%, 2px 100%);
        transform: none;
      }

      .article-scroll .scroll-paper::before,
      .article-scroll .scroll-paper::after {
        transform: scaleX(0.66);
        opacity: 0.76;
      }

      .article-scroll .post-main,
      .article-scroll .post-tags {
        opacity: 1;
        transform: none;
      }
    }
  </style>
<BaseLayout title="墨笺" description="墨笺，一处记录日常、山川、旧书与生活片段的个人博客。">
  <div id="top">
    <section class="hero" aria-labelledby="hero-title">
      <div class="shell hero-grid">
        <div class="hero-content">
          <p class="hero-eyebrow">一方墨笺 · 记于人间</p>
          <h1 class="hero-title" id="hero-title">墨笺有余闲<span class="accent-dot">。</span></h1>
          <p class="hero-subtitle">写日常，也写山川与旧书</p>
          <div class="hero-intro">
            这里收拢寻常日子里的微光。<br />
            也留下山川、旧书与缓慢生长的心事。
          </div>
          <div class="hero-actions" aria-label="页面入口">
            <a class="action-primary" href="#articles">阅览文章</a>
            <a class="action-secondary" href="#about">关于此间</a>
          </div>
        </div>

        <div class="hero-side">
          <div class="hero-vertical-copy" aria-hidden="true">风来疏竹　雨过远山</div>
          <aside class="hero-note" aria-label="案上近况">
            <div class="hero-note-head">
              <strong>案上</strong>
              <time datetime="2026-07">季夏 · 二〇二六</time>
            </div>
            <dl>
              <div>
                <dt>正在搭建</dt>
                <dd>继续打磨这座 Astro 个人博客</dd>
              </div>
              <div>
                <dt>近日整理</dt>
                <dd>山中一日、雨落窗前与旧书</dd>
              </div>
            </dl>
          </aside>
        </div>
      </div>

      <div class="hero-landscape" aria-hidden="true">
        <Image
          src={inkMountains}
          alt=""
          widths={[960, 1280, 1672]}
          sizes="100vw"
          loading="eager"
          fetchpriority="high"
        />
      </div>
      <div class="scroll-hint" aria-hidden="true">向下</div>
    </section>

    <div class="home-layout">
    <section class="section study-index" aria-labelledby="study-title">
      <div class="shell study-grid">
        <div class="study-intro reveal">
          <div>
            <p class="section-kicker">THE STUDY INDEX</p>
            <h2 class="study-title" id="study-title">书斋目录</h2>
            <p>把日常与见闻，按四条线索慢慢归置。</p>
          </div>
          <div class="study-mini-painting" aria-hidden="true">
            <svg viewBox="0 0 360 190" xmlns="http://www.w3.org/2000/svg">
              <defs>
                <filter id="miniSoft"><feGaussianBlur stdDeviation="1.2" /></filter>
              </defs>
              <circle cx="286" cy="50" r="20" fill="#a44434" fill-opacity=".12" />
              <path d="M0 140C63 120 87 86 138 96C184 105 196 137 241 125C281 115 312 86 360 82V190H0Z" fill="#586258" fill-opacity=".09" filter="url(#miniSoft)" />
              <path d="M0 164C65 145 109 158 159 137C215 113 255 151 306 141C328 137 344 128 360 119V190H0Z" fill="#303631" fill-opacity=".1" />
              <path d="M259 128l5-28 4 28zm10 1 4-22 3 22z" fill="#303631" fill-opacity=".2" />
              <path d="M46 158c36-12 63-13 96-5" fill="none" stroke="#303631" stroke-opacity=".18" />
            </svg>
          </div>
        </div>

        <nav class="index-list reveal" aria-label="内容分类">
          <a class="index-item" href="#articles">
            <span class="index-number">壹</span>
            <span class="index-name">日常</span>
            <span class="index-desc">寻常烟火与片刻心绪</span>
            <span class="index-count">01 篇</span>
          </a>
          <a class="index-item" href="#articles">
            <span class="index-number">贰</span>
            <span class="index-name">行旅</span>
            <span class="index-desc">山川远近与沿途所见</span>
            <span class="index-count">01 篇</span>
          </a>
          <a class="index-item" href="#articles">
            <span class="index-number">叁</span>
            <span class="index-name">读书</span>
            <span class="index-desc">旧书新页与字里光阴</span>
            <span class="index-count">01 篇</span>
          </a>
          <a class="index-item" href="#about">
            <span class="index-number">肆</span>
            <span class="index-name">摄影</span>
            <span class="index-desc">光影流转与留住片刻</span>
            <span class="index-count">00 篇</span>
          </a>
        </nav>
      </div>
      <svg class="brush-divider" viewBox="0 0 960 42" preserveAspectRatio="none" aria-hidden="true">
        <path d="M10 23C132 19 228 26 346 21C470 15 592 28 726 20C814 15 888 20 950 17" fill="none" stroke="#485149" stroke-width="4" stroke-linecap="round" opacity=".42" />
        <path d="M75 27C222 25 344 31 486 25C620 20 733 27 861 23" fill="none" stroke="#485149" stroke-width="1" opacity=".35" />
      </svg>
    </section>

    <section class="section latest" id="articles" aria-labelledby="latest-title">
      <div class="shell">
        <div class="section-heading reveal">
          <div>
            <p class="section-kicker">RECENT NOTES</p>
            <h2 class="section-title" id="latest-title">近来所记</h2>
          </div>
          <p class="section-note">新札三卷，横陈案上。行旅、日常与旧书各成一卷，也把远山与墨痕留在纸面。</p>
        </div>

        <div class="scroll-list" id="archive">
          <article class="article-scroll reveal">
            <div class="scroll-axis" aria-hidden="true"></div>
            <div class="scroll-paper">
              <div class="scroll-ink" aria-hidden="true">
                <svg viewBox="0 0 1100 300" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
                  <defs><filter id="postSoft1"><feGaussianBlur stdDeviation="4" /></filter></defs>
                  <circle cx="950" cy="82" r="32" fill="#a44434" fill-opacity=".08" />
                  <path d="M690 228C760 184 804 197 854 157C900 120 936 147 977 177C1015 205 1054 183 1100 150V300H680Z" fill="#586258" fill-opacity=".09" filter="url(#postSoft1)" />
                  <path d="M760 260C827 225 878 241 925 214C976 185 1022 221 1100 194V300H740Z" fill="#2f3530" fill-opacity=".07" />
                  <path d="M80 70c72-18 121-10 180 10" fill="none" stroke="#454c46" stroke-opacity=".07" stroke-width="12" filter="url(#postSoft1)" />
                </svg>
              </div>
              <span class="scroll-seal" aria-hidden="true">卷<br />壹</span>
              <div class="post-meta" data-index="01">
                <time datetime="2026-07-15">2026 年 7 月 15 日</time>
                <span class="category">行旅</span>
                <span class="reading-time">约 6 分钟</span>
              </div>
              <div class="post-main">
                <h3 class="post-title"><a href={`${BASE}posts/shan-zhong-yi-ri/`} aria-label="阅读：山中一日">山中一日</a></h3>
                <p class="post-excerpt">晨起入山，雾气未散。林间鸟鸣清越，时间也慢了下来。</p>
              </div>
              <div class="post-tags" aria-label="文章标签">
                <span>山水</span>
                <span>随记</span>
              </div>
            </div>
            <div class="scroll-axis" aria-hidden="true"></div>
          </article>

          <article class="article-scroll reveal">
            <div class="scroll-axis" aria-hidden="true"></div>
            <div class="scroll-paper">
              <div class="scroll-ink" aria-hidden="true">
                <svg viewBox="0 0 1100 300" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
                  <defs><filter id="postSoft2"><feGaussianBlur stdDeviation="4" /></filter></defs>
                  <path d="M0 230C76 196 126 205 190 161C251 120 292 154 338 184C379 211 422 190 478 151V300H0Z" fill="#586258" fill-opacity=".075" filter="url(#postSoft2)" />
                  <path d="M0 264C86 230 153 249 218 216C279 186 335 224 414 198V300H0Z" fill="#303631" fill-opacity=".06" />
                  <path d="M928 88c35-18 71-18 110-2" fill="none" stroke="#495149" stroke-opacity=".08" stroke-width="14" filter="url(#postSoft2)" />
                  <circle cx="134" cy="78" r="26" fill="#a44434" fill-opacity=".065" />
                </svg>
              </div>
              <span class="scroll-seal" aria-hidden="true">卷<br />贰</span>
              <div class="post-meta" data-index="02">
                <time datetime="2026-06-28">2026 年 6 月 28 日</time>
                <span class="category">日常</span>
                <span class="reading-time">约 5 分钟</span>
              </div>
              <div class="post-main">
                <h3 class="post-title"><a href={`${BASE}posts/yu-luo-chuang-qian/`} aria-label="阅读：雨落窗前">雨落窗前</a></h3>
                <p class="post-excerpt">雨点敲打屋檐，落在窗前的瓦上，一本书、一盏茶，便是安静的时光。</p>
              </div>
              <div class="post-tags" aria-label="文章标签">
                <span>雨天</span>
                <span>日常</span>
              </div>
            </div>
            <div class="scroll-axis" aria-hidden="true"></div>
          </article>

          <article class="article-scroll reveal">
            <div class="scroll-axis" aria-hidden="true"></div>
            <div class="scroll-paper">
              <div class="scroll-ink" aria-hidden="true">
                <svg viewBox="0 0 1100 300" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
                  <defs><filter id="postSoft3"><feGaussianBlur stdDeviation="4" /></filter></defs>
                  <path d="M650 244C723 211 777 224 834 190C895 154 937 192 986 209C1028 224 1064 213 1100 194V300H630Z" fill="#586258" fill-opacity=".08" filter="url(#postSoft3)" />
                  <path d="M720 270C800 241 849 252 908 229C970 205 1017 238 1100 218V300H700Z" fill="#303631" fill-opacity=".055" />
                  <path d="M128 84c64-22 121-17 185 3" fill="none" stroke="#4b534c" stroke-opacity=".08" stroke-width="15" filter="url(#postSoft3)" />
                  <path d="M228 132c16-9 28-8 42 2m24-17c13-7 24-6 35 2" fill="none" stroke="#343a35" stroke-opacity=".14" stroke-linecap="round" />
                </svg>
              </div>
              <span class="scroll-seal" aria-hidden="true">卷<br />叁</span>
              <div class="post-meta" data-index="03">
                <time datetime="2026-05-12">2026 年 5 月 12 日</time>
                <span class="category">读书</span>
                <span class="reading-time">约 3 分钟</span>
              </div>
              <div class="post-main">
                <h3 class="post-title"><a href={`${BASE}posts/jiu-shu-yu-cha/`} aria-label="阅读：旧书与茶">旧书与茶</a></h3>
                <p class="post-excerpt">翻开一本带着旧纸气味的书，也像重新遇见了从前的自己。</p>
              </div>
              <div class="post-tags" aria-label="文章标签">
                <span>阅读</span>
                <span>茶</span>
              </div>
            </div>
            <div class="scroll-axis" aria-hidden="true"></div>
          </article>
        </div>

        <div class="all-posts reveal">
          <a href={`${BASE}archive/`}>查看全部文章</a>
        </div>
      </div>
    </section>

    <section class="section desk" aria-labelledby="desk-title">
      <div class="shell">
        <div class="section-heading reveal">
          <div>
            <p class="section-kicker">ON THE DESK</p>
            <h2 class="section-title" id="desk-title">案头近况</h2>
          </div>
          <p class="section-note">不只保存已经写完的文章，也留下正在发生的过程。墨色稍深，像夜里案上的一盏灯。</p>
        </div>

        <div class="desk-grid">
          <article class="now-panel reveal">
            <div class="panel-mark" aria-hidden="true">今</div>
            <div class="now-head">
              <span>此刻</span>
              <time datetime="2026-07-15">2026.07.15</time>
            </div>
            <div class="now-list">
              <div class="now-row">
                <span class="now-label">正在做</span>
                <strong>继续打磨墨笺的视觉与内容</strong>
                <span class="now-state">进行中</span>
              </div>
              <div class="now-row">
                <span class="now-label">正在读</span>
                <strong>山川、旧书与日常写作的零散笔记</strong>
                <span class="now-state">缓慢读</span>
              </div>
              <div class="now-row">
                <span class="now-label">正在学</span>
                <strong>用更少的元素，建立更稳定的页面秩序</strong>
                <span class="now-state">持续中</span>
              </div>
            </div>
          </article>

          <article class="fragment-panel reveal">
            <div class="fragment-art" aria-hidden="true">
              <svg viewBox="0 0 540 310" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
                <defs>
                  <filter id="deskSoft"><feGaussianBlur stdDeviation="3" /></filter>
                  <linearGradient id="deskMist" x1="0" y1="0" x2="0" y2="1">
                    <stop offset="0" stop-color="#eef0e9" stop-opacity=".05" />
                    <stop offset="1" stop-color="#eef0e9" stop-opacity=".2" />
                  </linearGradient>
                </defs>
                <circle cx="402" cy="74" r="34" fill="#bd6756" fill-opacity=".24" />
                <path d="M0 205C82 165 144 182 211 135C276 91 326 130 377 159C427 187 471 159 540 119V310H0Z" fill="url(#deskMist)" filter="url(#deskSoft)" />
                <path d="M0 252C92 215 164 236 233 198C302 161 356 206 410 226C452 241 497 223 540 202V310H0Z" fill="#eef0e9" fill-opacity=".075" />
                <path d="M76 260C142 234 199 242 256 220C326 193 378 219 440 241C476 253 508 247 540 234" fill="none" stroke="#eef0e9" stroke-opacity=".17" />
                <path d="M92 202V72M92 72H320M320 72V205" fill="none" stroke="#eef0e9" stroke-opacity=".2" stroke-width="2" />
                <path d="M207 72V202" fill="none" stroke="#eef0e9" stroke-opacity=".12" />
                <path d="M430 238l7-38 6 38zm12 1 5-29 5 29z" fill="#eef0e9" fill-opacity=".16" />
              </svg>
            </div>
            <div class="fragment-copy">
              <p class="fragment-label">一页小记</p>
              <blockquote>写作并不总是为了给出答案。很多时候，它只是把走过的路重新照亮。</blockquote>
              <p class="fragment-source">—— 记于墨笺之间</p>
            </div>
          </article>
        </div>
      </div>
    </section>

    <section class="section about" id="about" aria-labelledby="about-title">
      <div class="shell about-grid">
        <div class="about-copy reveal">
          <p class="section-kicker">ABOUT THIS PLACE</p>
          <h2 class="section-title" id="about-title">关于此间</h2>
          <div class="about-text">
            <p>我喜欢记录日常，也喜欢山川与旧书。</p>
            <p>这个网站会保存沿途所见、读书所得，以及一些没有明确分类的想法。</p>
          </div>
          <div class="about-signature">愿每一页都有来处</div>
          <div class="about-topics" aria-label="长期记录主题">
            <span>代码</span>
            <span>器物</span>
            <span>光影</span>
            <span>山川</span>
          </div>
        </div>

        <div class="ink-orbit reveal" aria-hidden="true">
          <div class="ink-circle"></div>
          <svg class="bamboo" viewBox="0 0 340 340" xmlns="http://www.w3.org/2000/svg">
            <g fill="none" stroke="#586258" stroke-linecap="round">
              <path d="M108 294C131 236 151 177 173 78" stroke-width="5" opacity=".72" />
              <path d="M170 93C204 111 228 135 250 168" stroke-width="2.7" opacity=".58" />
              <path d="M148 170C114 153 90 132 70 101" stroke-width="2.5" opacity=".5" />
              <path d="M129 226C160 218 187 221 215 236" stroke-width="2.4" opacity=".5" />
              <path d="M116 267C92 254 74 237 59 214" stroke-width="2.1" opacity=".42" />
            </g>
            <g fill="#586258">
              <ellipse cx="218" cy="125" rx="9" ry="32" transform="rotate(-42 218 125)" opacity=".44" />
              <ellipse cx="246" cy="160" rx="8" ry="28" transform="rotate(-30 246 160)" opacity=".37" />
              <ellipse cx="98" cy="132" rx="9" ry="33" transform="rotate(43 98 132)" opacity=".39" />
              <ellipse cx="70" cy="101" rx="8" ry="28" transform="rotate(38 70 101)" opacity=".31" />
              <ellipse cx="181" cy="224" rx="9" ry="33" transform="rotate(-74 181 224)" opacity=".39" />
              <ellipse cx="212" cy="235" rx="8" ry="28" transform="rotate(-63 212 235)" opacity=".32" />
              <ellipse cx="87" cy="252" rx="8" ry="28" transform="rotate(51 87 252)" opacity=".32" />
              <ellipse cx="59" cy="215" rx="7" ry="25" transform="rotate(42 59 215)" opacity=".26" />
            </g>
            <g stroke="#f2eee3" stroke-width="1.6" opacity=".42">
              <path d="M148 169l14 4" />
              <path d="M160 126l14 4" />
              <path d="M132 219l13 4" />
            </g>
          </svg>
          <div class="about-seal">山窗<br />小记</div>
        </div>
      </div>
    </section>
    </div>
  </div>

  <script>
    // 元素进入视口后淡入。系统关闭动画时，CSS 会直接显示内容。
    const revealItems = document.querySelectorAll(".reveal");
    const reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

    if (reduceMotion || !("IntersectionObserver" in window)) {
      revealItems.forEach((item) => item.classList.add("is-visible"));
    } else {
      const observer = new IntersectionObserver(
        (entries, currentObserver) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("is-visible");
              currentObserver.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.12, rootMargin: "0px 0px -7% 0px" }
      );

      revealItems.forEach((item) => observer.observe(item));
    }
  </script>
</BaseLayout>
