<footer>
  <div class="page-shell footer-inner">
    <span class="rule" aria-hidden="true"></span>
    <p>墨笺 · 记于人间</p>
    <span class="rule" aria-hidden="true"></span>
  </div>
</footer>

<style>
  footer {
    margin-top: 6rem;
    border-bottom: 10px solid var(--color-pine);
  }

  .footer-inner {
    display: grid;
    min-height: 8rem;
    grid-template-columns: minmax(2rem, 8rem) auto minmax(2rem, 8rem);
    align-items: center;
    justify-content: center;
    gap: 1.2rem;
    border-top: 1px solid rgb(67 83 73 / 20%);
  }

  p {
    margin: 0;
    color: var(--color-ink-soft);
    letter-spacing: 0.2em;
  }

  .rule {
    height: 1px;
    background: rgb(67 83 73 / 25%);
  }

  @media (max-width: 560px) {
    footer {
      margin-top: 4rem;
    }

    .footer-inner {
      min-height: 6rem;
      grid-template-columns: 2rem auto 2rem;
      gap: 0.7rem;
    }

    p {
      font-size: 0.9rem;
      letter-spacing: 0.12em;
    }
  }
</style>
